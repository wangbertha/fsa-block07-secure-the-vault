// Create a string with the intro message
const introMessage = "You have received this message because you have been chosen to open an important vault. Here is the secret combination:";

// Assign 3 variables
const numA = 5 + 5;
const numB = numA * 4;
const numC = numB - 1;

// Display text and vault codes together
console.log(`${introMessage} ${numA} - ${numB} - ${numC}`);
alert(`${introMessage} ${numA} - ${numB} - ${numC}`)